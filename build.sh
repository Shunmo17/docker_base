COMPOSE_DOCKER_CLI_BUILD=1 \
DOCKER_BUILDKIT=1 \
docker-compose build \
    --parallel

# COMPOSE_DOCKER_CLI_BUILD=1 \
# DOCKER_BUILDKIT=1 \
# docker-compose build \
#     --no-cache \
#     --parallel
